{% if changes|length %}

{% for change in changes %}
{% if change.source.isCommit %}
- {% if not change.source.hasUrl %}{{ change.source.id | slice(0, 6) }}{% else %}[{{ change.source.id | slice(0, 6) }}]({{ change.source.url }}){% endif %} - **{{ change.description|trim|raw }}** - [@{{ change.source.user }}]({{ change.source.userUrl }})
{% elseif change.source.isPullRequest %}
- {% if not change.source.hasUrl %}{{ change.source.id }}{% else %}[#{{ change.source.id }}]({{ change.source.url }}){% endif %} - **{{ change.description|trim|raw }}** - [@{{ change.source.user }}]({{ change.source.userUrl }})
{% endif %}
{% endfor %}
{% endif %}